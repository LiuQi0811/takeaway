server:
  port: 8080
spring:
  application:
    name: takeaway
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/takeaway?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: takeaway
      password: 5nSX2tEEM6fD3AKX

#  RabbitMQ 基础配置
  rabbitmq:
    host: 172.16.100.26 # 地址
    port: 5673 # 端口号
    username: guest #用户名
    password: guest #密码
    virtual-host: /
    listener:
      simple:
        prefetch: 1 #设置prefetch 控制欲取消息 上限


mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID


